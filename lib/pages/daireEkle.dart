import 'package:flutter/material.dart';

class DaireEkle extends StatelessWidget {
  const DaireEkle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
  return Scaffold(
    appBar: AppBar(
      title: Text("Aidat Detay Sayfası"),
    ),
    body: Text("lsjdn"),
  );
  }

}